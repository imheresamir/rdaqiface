[package]
name = "rdaqiface"
version = "0.1.0"
authors = ["samir"]
edition = "2018"
build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2.66"

[build-dependencies]
cmake = "0.1.42"
